# `@agency/cms-module-redirects`

Optional redirect registry for Agency CMS with loop/duplicate detection and
deterministic CSV/JSON portability. Uninstall retains redirect rows so routes
do not silently regress; an operator may export and purge them explicitly.
