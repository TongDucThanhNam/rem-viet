# `@agency/cms-module-forms`

Official schema-driven forms and leads module. It validates submissions on the
server, exposes spam/rate-limit adapters, records consent, dispatches durable
notification/webhook events, and exports portable lead data.
